name: coentrepreneurs
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  
  # Firebase - Versions stables pour Flutter 3.38
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2
  
  # State management
  provider: ^6.1.0
  
  # Navigation
  go_router: ^17.0.1
  
  # UI
  google_fonts: ^8.0.0
  
  # Utils
  intl: ^0.20.2
  url_launcher: ^6.3.2

  # pour le web :
  universal_html: ^2.0.0

  # Pour jouer l'audio
  just_audio: ^0.9.36

  firebase_storage: ^13.0.6  # ← AJOUTER
  image_picker: ^1.0.0      # ← AJOUTER

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/charte_ethique.jpg
    - assets/audio/charte_audio.mp3